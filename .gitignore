node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/test-results
/playwright-report

# One package manager: npm (package-lock.json). Other lockfiles are never committed.
bun.lock
bun.lockb
yarn.lock
pnpm-lock.yaml

# Per-machine agent settings
.claude/settings.local.json
